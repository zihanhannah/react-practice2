import React from 'react';

const validation = (props) => {
   
    return (
        <div >
            <p>{props.length}</p>
            <p>{props.text}</p>
        </div>
    )

}
export default validation;